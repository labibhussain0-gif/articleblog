{
  "name": "the-daily-pulse-studio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "XDG_CONFIG_HOME=/tmp/sanity_config sanity dev",
    "build": "XDG_CONFIG_HOME=/tmp/sanity_config sanity build",
    "deploy": "XDG_CONFIG_HOME=/tmp/sanity_config sanity deploy",
    "deploy-graphql": "XDG_CONFIG_HOME=/tmp/sanity_config sanity graphql deploy"
  },
  "dependencies": {
    "@sanity/vision": "^3.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "sanity": "^3.0.0",
    "styled-components": "^6.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "typescript": "5.1.3"
  }
}
